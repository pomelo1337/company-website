/* =========================================================
   POMELO ETF 量化 - 全局配置
   ---------------------------------------------------------
   apiBase：后端服务地址（可选）。
   - 留空 "" = 纯静态模式（当前）：前端直连东方财富公开接口，
     持仓/自选保存在本机浏览器，无需任何服务器
   - 填写地址 = 后端模式（需自行部署 server/ 目录）
   ========================================================= */
window.POMELO_CONFIG = {
  apiBase: ""
};
